{
  "name": "wangyiyun",
  "version": "1.0.0",
  "private": true,
  "description": "Lumen — NetEase Cloud Music automation console (QR login, auto sign-in, listen-count farming)",
  "scripts": {
    "install:all": "npm --prefix server install && npm --prefix web install",
    "build": "npm --prefix web install && npm --prefix web run build",
    "start": "npm --prefix server start",
    "dev:server": "npm --prefix server run dev",
    "dev:web": "npm --prefix web run dev",
    "lint": "npm --prefix web run lint",
    "deploy": "npm --prefix server install && npm run build && npm run start"
  },
  "engines": {
    "node": ">=18"
  }
}
